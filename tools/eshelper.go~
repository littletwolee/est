package tools

import (
	"gopkg.in/olivere/elastic.v3"
)

var (
	eshost = beego.AppConfig.String("eshost")
	esport = beego.AppConfig.String("esport")
	
)

type ESHelper struct{}

func (e *ESHelper)GetClient() 
	
}
